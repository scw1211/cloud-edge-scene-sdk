# 公共 SDK 依赖
jsonschema>=4.21,<5
numpy>=1.24,<3
scikit-learn>=1.3,<2
scipy>=1.10,<2
joblib>=1.3,<2

# Jetson 上请使用 NVIDIA/JetPack 对应的 PyTorch wheel，不要直接安装桌面版 torch。
# x86_64 服务器或 WSL 可按本机 CUDA 版本安装 PyTorch。
